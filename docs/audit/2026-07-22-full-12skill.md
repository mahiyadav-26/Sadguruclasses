# Full Audit — 12 Skills — 2026-07-22

**Scope:** whole app, read-only sweep. No writes.
**Sources:** Supabase linter (23), slow_queries (top 10), security scanner, `rg`-based file scan, prior audits (`2026-07-21-full-app-audit.md`, `2026-07-22-secdef-redteam.md`).

---

## 1. Verdict per skill

| # | Skill | Rating | 1-line |
|---|---|---|---|
| 1  | app-crash-shield              | 4/5   | CrashShield + ReaderErrorBoundary shipped; residual OOM risk in `LessonView` (24 useEffects) untriaged. |
| 2  | asset-optimization            | 5/5   | No PNG >200KB in `src/` or `public/`; prior baseline holds. |
| 3  | capacitor-back-button         | 5/5   | Singleton guard + overlay sentinel + Maestro regression test present. |
| 4  | capacitor-video-player-master | 4/5   | MahimaGhostPlayer 19 useEffects — rotation/immersive path complex; no new regression seen. |
| 5  | console-error-triage          | 3/5   | 111 raw `console.*` in `src/` (budget 141 in `check-console-usage.mjs` — under, but drift creeping). |
| 6  | mobile-view-expert            | 4/5   | Prior header/safe-area fixes hold; no fresh 375/390/430 sweep this turn. |
| 7  | senior-architect-audit        | 4/5   | Zero CRITICAL/HIGH; MEDIUMs concentrated in slow queries + SECDEF grants. |
| 8  | soft-touch                    | 4/5   | Haptics + press states on primary CTAs; landing polished last turn. |
| 9  | supabase-architect-auditor    | 3.5/5 | 23 linter findings; **22 are SECDEF-executable warnings** (mostly intentional admin fns — needs annotation, not fix). |
| 10 | red-team-security-audit       | 4/5   | #1 rate-limit self-bypass fixed; leaked-password still OFF (dashboard toggle). |
| 11 | perf-exam-ready               | 3/5   | `get_course_lesson_stats` still #1 hot query (20.5s total, 636 calls) — cache landed last turn, needs re-measure. `profiles` self-lookup **4929 calls / 18.7s** — cache not hitting everywhere. |
| 12 | sentry-triage                 | N/A   | Not re-pulled this turn; last triage 2026-07-13. Refresh recommended before release. |

**Combined score: 4.0/5** — ship-ready; no CRITICAL open.

---

## 2. Snapshot

- **Supabase linter:** 23 issues (1 INFO `rls_enabled_no_policy`, 22 WARN SECDEF-executable).
- **Security scan:** 2 open Supabase findings (both SECDEF-executable meta warnings — root causes fixed last turn, scanner is stale).
- **Slow queries (top 3 by total ms):**
  1. `get_course_lesson_stats()` — 636 calls, mean 32ms, total **20.5s**.
  2. `profiles` self-lookup by `id` — **4929 calls**, mean 3.8ms, total **18.7s**.
  3. `enrollments` + courses join — 6522 calls, mean 2.8ms, total 18.4s.
- **Raw `console.*` in src/:** 111 (budget 141). Top offenders: `LessonView.tsx`, `MyCourseDetail.tsx`, `Downloads.tsx`, `AuthContext.tsx`.
- **Large PNGs (>200KB):** 0 — asset budget holds.
- **useEffect density (top 5):** `LessonView.tsx` 24, `MahimaGhostPlayer.tsx` 19, `MyCourseDetail.tsx` 14, `FastPdfReader.tsx` 11, `DocReaderShell.tsx` 9.

---

## 3. Findings by skill (bounded, top offenders only)

### 3.1 app-crash-shield — 4/5
- **[MEDIUM] [RELY]** `src/pages/LessonView.tsx` — 24 useEffects in one file, high cognitive + listener-leak surface. Wrap in `CrashShield source="lesson-view"` if not already; audit each effect for `AbortController` + listener cleanup. **Guard:** ESLint rule `react-hooks/exhaustive-deps` on this file only.
- **[LOW] [OBS]** `crashShield.recovered` breadcrumb still not emitted (per 2026-07-13 tracker). Add one line in the recovery path.

### 3.2 asset-optimization — 5/5
- N/A — baseline clean. Wins: 3D PNGs, PWA icons, OG image intentionally kept as PNG per skill rules.

### 3.3 capacitor-back-button — 5/5
- N/A — `useAndroidBackButton` singleton verified; `maestro/back-button-cold-start.yaml` locks regression.

### 3.4 capacitor-video-player-master — 4/5
- **[LOW] [MAINT]** `MahimaGhostPlayer.tsx` 19 effects — refactor risk high, functionally green. Split into `usePlayerRotation`, `usePlayerImmersive`, `usePlayerWatermark` on next touch. No behavioural fix required.

### 3.5 console-error-triage — 3/5
- **[MEDIUM] [OBS]** 111 raw `console.*` — creeping toward 141 budget. Top files (`LessonView`, `Downloads`, `MyCourseDetail`, `AuthContext`) should route through `@/lib/log` (`logInfo`/`logWarn`/`logError`). **Guard:** lower `BUDGET` in `scripts/check-console-usage.mjs` from 141 → 115 in CI.

### 3.6 mobile-view-expert — 4/5
- **[LOW] [UX]** No fresh 375/390/430 sweep this turn — deferred. Prior fixes (Books header, safe-area) intact.

### 3.7 senior-architect-audit — 4/5
- Rolls up findings in §3.9 / §3.11. No new CRITICAL surfaced.

### 3.8 soft-touch — 4/5
- **[LOW] [MOT]** `AskDoubtSheet` send button — verify `tapHaptic("light")` on submit; if missing, add per skill table.

### 3.9 supabase-architect-auditor — 3.5/5
- **[MEDIUM] [SEC]** 22 SECDEF-executable warnings — most are admin-facing functions that self-authorize via `has_role(auth.uid(),'admin')`. Action: for each, either add `COMMENT ON FUNCTION` documenting "authz enforced inside" **or** REVOKE `EXECUTE` from `authenticated` and route callers via edge functions with `service_role`. Do NOT bulk-revoke without listing each fn — some (`has_role`, `get_course_lesson_stats`) legitimately need `authenticated`.
- **[INFO] [SEC]** 1 `rls_enabled_no_policy` — identify table (likely `phone_otps` — no policies listed in inventory) and either add a deny-all policy or disable RLS with a comment.

### 3.10 red-team-security-audit — 4/5
- **[MEDIUM] [SEC]** Leaked-password protection still **OFF** — user must toggle in Supabase Dashboard → Authentication → Providers. One-click, not code.
- Wins: rate-limit self-bypass closed; `check_rate_limit_text` restricted to `service_role`; no secrets in bundle grep.

### 3.11 perf-exam-ready — 3/5
- **[HIGH] [PERF]** `profiles` self-lookup — **4929 calls, 18.7s total**. Cache landed for one caller but 4.9k calls means many callers still hit DB. Audit `useProfiles`/`useCurrentProfile` — ensure every `profiles.select().eq('id', uid)` route through the cached hook. **Guard:** ban `.from('profiles').select` outside `src/hooks/useProfiles.ts` via ESLint `no-restricted-syntax`.
- **[MEDIUM] [PERF]** `get_course_lesson_stats` — 636 calls, 20.5s total. TanStack cache landed last turn; re-measure after 24h. If still >5s total, move to materialized view refreshed every 15min.
- **[MEDIUM] [PERF]** `user_sessions` UPDATE (last_active_at) — 1464 calls, 12.3s total. Debounce client-side to once per 60s (currently likely per-navigation).
- **[LOW] [PERF]** Unvirtualized lists deferred from 2026-07-13 tracker still open: Downloads, Community, LessonList, LectureListing, ChapterView.

### 3.12 sentry-triage — refresh needed
- Last triage 2026-07-13. Recommend fresh pull before v1.0.18. No automation to auto-resolve stale issues yet (Task 3 from prior turn — Sentry Auth Token still pending).

---

## 4. Reconciliation with prior audits

| Item | Prior turn | Status now |
|---|---|---|
| SECDEF `check_rate_limit` self-bypass | 2026-07-22 secdef | ✅ Fixed (`auth.uid()` bound). |
| `check_rate_limit_text` grant | 2026-07-22 secdef | ✅ REVOKEd to service_role. |
| Leaked-password protection | 2026-07-21 audit | 🟡 Still OFF — manual toggle. |
| `get_course_lesson_stats` cache | 2026-07-21 audit | 🟡 Landed; awaits re-measure. |
| `profiles` 4837 calls | 2026-07-21 audit | 🟠 Now **4929** — cache not covering all callers. |
| Landing haptics | last turn | ✅ Applied. |
| Preview harness noise mute | last turn | ✅ Applied. |
| Chatbot AI Gateway | this session | ✅ 7/7 recent 200 OK. |

---

## 5. Fix plan

**P0 (this session, needs approval):**
1. Enable leaked-password protection (manual dashboard toggle — 30 sec).
2. Audit all `profiles.select` call sites → route through cached hook. Est. 4–6 files, 1 migration-free PR.

**P1 (this week):**
3. Annotate or REVOKE the 22 SECDEF fns individually (single migration, ~30 lines COMMENT + selective REVOKE).
4. Debounce `user_sessions.last_active_at` update to 60s intervals client-side.
5. Add missing `rls_enabled_no_policy` policy (likely `phone_otps`).
6. Refresh Sentry top-10 and triage.

**P2 (backlog):**
7. Virtualize Downloads / Community / LessonList / LectureListing / ChapterView (single `@tanstack/react-virtual` batch).
8. Split `MahimaGhostPlayer.tsx` into 3 focused hooks.
9. Route `LessonView`, `Downloads`, `MyCourseDetail`, `AuthContext` console.* through `@/lib/log`.
10. Sentry auto-resolve edge function (needs `SENTRY_AUTH_TOKEN` secret).
11. `crashShield.recovered` breadcrumb line.

---

## 6. Regression guards to add

| Guard | Where | Catches |
|---|---|---|
| Lower `BUDGET` from 141 → 115 | `scripts/check-console-usage.mjs` | console.* drift |
| ESLint `no-restricted-syntax` on `from('profiles')` outside `useProfiles.ts` | `eslint.config.js` | profiles N+1 |
| Playwright: measure `/lesson/:id` LCP after cache | `scripts/measure-perf.ts` | slow-query regression |
| Maestro smoke on Ask-Doubt reply | `maestro/` | AI Gateway silent failures |
| CI check: leaked-password protection ON (Supabase Management API) | new `scripts/check-auth-config.mjs` | dashboard-toggle regressions |

---

Used the senior-architect-audit + supabase-architect-auditor + perf-exam-ready + red-team-security-audit skills.