# Perf Report — Exam-Ready Pass — 2026-07-17

**Verdict:** ship. Bundle already well within budget; added idle prefetch for the hottest user path (Courses → MyCourseDetail → LessonView → PDF).

## Baseline vs After

| Metric | Before | After | Δ |
|---|---|---|---|
| Initial entry gzip | 102.7KB | 102.7KB | 0 (already under 180KB budget) |
| LessonView chunk gzip | 27.1KB | 27.1KB | 0 |
| vendor-pdf gzip | 119.7KB | 119.7KB | lazy, unchanged |
| Perceived LessonView cold open (from Courses) | full network fetch on tap | chunk already warm in cache | −chunk RTT (~200–800ms on 3G) |
| Perceived PDF drawer open (from MyCourseDetail) | full FastPdfReader fetch on tap | warm | −chunk RTT |

Initial entry didn't move because prefetch runs **after** first paint during `requestIdleCallback` — that's the point. TTI on `/courses` and `/my-courses/:id` unchanged; **transition to the next screen is what gets faster.**

## Changes applied

**Runtime — idle prefetch (`Lane 3`):**
- `src/lib/idlePrefetch.ts` — new util. `requestIdleCallback` with 2s timeout, `setTimeout(400ms)` fallback for iOS WebView. Deduped via `WeakSet`. Errors swallowed (lazyWithRetry handles the real load).
- `src/pages/Courses.tsx` — on mount, prefetch `MyCourseDetail` + `LessonView`.
- `src/pages/MyCourseDetail.tsx` — on mount, prefetch `LessonView` + `LessonAttachmentsSheet` + `FastPdfReader`.

## Audit findings (no code change needed — already correct)

- **Assets (Lane 2):** LCP preload present (`<link rel="preload" as="image" href="/brand/nb-mark.webp" fetchpriority="high">`). Fonts preloaded. No duplicate raster assets remaining after the earlier consolidation pass.
- **Runtime (Lane 3):** Every `src/pages/*` uses `lazyWithRetry` — no bare `React.lazy`. Heavy vendor chunks (`html2pdf`, `eruda`, `vendor-pdf`, `vendor-md`, `vendor-sentry`) are all dynamic-imported at call site. Eruda loads only on `?debug=1` or `nb_admin_eruda=1` — non-admins never download the 146KB chunk. TanStack Query `staleTime` tuned per hook (site_settings 1h, hero_banners 5m, notices 5m, plans 1h, courses 5m, timetable 5m, platform stats 24h).
- **Backend (Lane 4):** No `.range(0, 999)` on growing tables. PDF hard rules intact (streaming on, in-app viewer only, IntersectionObserver page mount).
- **Ship (Lane 5):** Build passes, entry 102.7KB gz (budget 180KB), APK pipeline untouched.

## Pending (needs Supabase MCP access or explicit approval)

- **DB indexes verification** — `supabase--slow_queries` + linter not reachable this session. Recommend running `scripts/snapshot.sh` on next connected session and posting output for a targeted index-review PR. Hot paths to confirm are indexed:
  - `lessons(chapter_id, sort_order)`, `chapters(course_id, sort_order)`
  - `enrollments(user_id, course_id)`, `lesson_progress(user_id, lesson_id)`
  - `community_posts(created_at DESC)`, `messages(session_id, created_at)`
  - `lesson_pdfs(id) INCLUDE (file_url, file_size, version)`
- **`pdf-proxy` cache header spot check** — needs `curl_edge_functions` to verify `Cache-Control: public, max-age=31536000, immutable` on versioned URLs.

## Follow-ups (not in this pass)

- Virtualize `Messages`, `Downloads`, `LessonList` if row count grows > 30 in prod telemetry (requires `@tanstack/react-virtual` — deferred until we see the row counts justify +4KB gz).
- `LessonView` split into lazy children — deferred per earlier `capacitor-core` reasoning (video/PDF cleanup ordering risk).

## Hard rules kept intact

In-app PDFs only · streaming on · splash safety ≤ 2s · single back-button handler · no service worker · Sentry stays quiet · design tokens only · APK pipeline untouched.

Used the perf-exam-ready, senior-architect-audit, and capacitor-bun-apk-build skills.
